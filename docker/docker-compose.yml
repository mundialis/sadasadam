---
services:
  sadasadam:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
    # Modify the paths below to point to your local data and config directories
      - /path/on/host/to/data:/src/data
      - /path/on/host/to/config_dir:/src/config
    env_file:
      - .env
